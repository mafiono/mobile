import { makeStyles } from "@mui/styles";

const sFooter = makeStyles((theme) => ({
    base: {
        // paddingBottom: theme.spacing(5),
        minHeight: "100vh",
        position: "relative",
        background: "linear-gradient(89.53deg, #1F071E 0.94%, #351E4A 53.66%, #170517 99.59%)",
    },
})) as any;

export default sFooter;